def compareobjec(a,b):
    if a is b:
        pass
       # dosomething
    # a,b 是一个对象
    if a == b :
        pass
       # dosomething
    # a,b 值相同
    if type(a) is type(b):
        pass
        # dosomething
    #a,b具有相同的类型

# 检查对象类型
# type(s) is list
# isinstance(s,list) --> isinstance(object, type)